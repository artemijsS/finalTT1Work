


window.addEventListener('DOMContentLoaded', (event) => {
    const data = db;
    console.log(data)

    let actualState = {
        language: "en",
        currency: "eur"
    }



    //FILTERS
    $filterType = $('#type');
    $filetrPartner = $('#partner');


    //SORT POP UP
    $popUp = $('.sort-pop-up');
    // $popUp.css("display", "none");
});